import { ResumeViewerType } from '../../CvViewer.types';

type CvViewerProps = {
  cv: ResumeViewerType;
};
export const Modern = ({ cv }: CvViewerProps) => {
  return <div>Render here modern template</div>;
};
